{{- if .Values.pinchflat.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pinchflat
  labels:
    app: pinchflat
spec:
  replicas: 1
  progressDeadlineSeconds: 600
  selector:
    matchLabels:
      app: pinchflat
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: pinchflat
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: kubernetes.io/hostname
                operator: In
                values:
                - hm60
{{- include "apps-chart.excludeNodeMatchExpressions" . | nindent 14 }}
      initContainers:
      - name: fix-permissions
        image: busybox:latest
        securityContext:
          runAsUser: 0
        command: ['sh', '-c']
        args:
          - |
            chown -R 1000:1000 /config
            chmod -R 755 /config
            chown -R 1000:1000 /downloads
            chmod -R 775 /downloads
        volumeMounts:
        - name: config
          mountPath: /config
        - name: downloads
          mountPath: /downloads
      containers:
      - name: pinchflat
        image: "{{ .Values.pinchflat.image.repository }}:{{ .Values.pinchflat.image.tag }}"
        env:
        - name: TZ
          value: {{ .Values.pinchflat.environment.TZ | quote }}
        resources:
          limits:
            memory: {{ .Values.pinchflat.resources.limits.memory }}
          requests:
            memory: {{ .Values.pinchflat.resources.requests.memory | default .Values.pinchflat.resources.limits.memory }}
            {{- if .Values.pinchflat.resources.requests.cpu }}
            cpu: {{ .Values.pinchflat.resources.requests.cpu }}
            {{- end }}
        ports:
        - containerPort: {{ .Values.pinchflat.ports.http }}
          protocol: TCP
        volumeMounts:
        - name: config
          mountPath: /config
        - name: downloads
          mountPath: /downloads
      securityContext:
        runAsUser: {{ include "apps-chart.userID" .Values.pinchflat.user | int }}
        runAsGroup: {{ include "apps-chart.groupID" .Values.pinchflat.user | int }}
        fsGroup: {{ include "apps-chart.groupID" .Values.pinchflat.user | int }}
      volumes:
      - name: config
        hostPath:
          path: /home/rkd/.nas/homelab/pinchflat/config
          type: DirectoryOrCreate
      - name: downloads
        hostPath:
          path: /home/rkd/.nas/YouTube
          type: DirectoryOrCreate
---
apiVersion: v1
kind: Service
metadata:
  name: pinchflat
  labels:
    app: pinchflat
spec:
  ports:
  - port: {{ .Values.pinchflat.ports.http }}
    targetPort: {{ .Values.pinchflat.ports.http }}
    protocol: TCP
  selector:
    app: pinchflat
{{- if and .Values.pinchflat.enabled .Values.pinchflat.ingress.enabled (gt (len .Values.pinchflat.ingress.hosts) 0) }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pinchflat
  labels:
    app: pinchflat
spec:
{{- if gt (len .Values.pinchflat.ingress.hosts) 0 }}
  tls:
  - hosts:
{{- range .Values.pinchflat.ingress.hosts }}
    - {{ . | quote }}
{{- end }}
    secretName: homelab-wildcard-h-tls
{{- end }}
  rules:
  {{- range .Values.pinchflat.ingress.hosts }}
  - host: {{ . | quote }}
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: pinchflat
            port:
              number: {{ $.Values.pinchflat.ingress.servicePort }}
  {{- end }}
{{- end }}
{{- end }}
