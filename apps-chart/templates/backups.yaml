{{- if .Values.backups.enabled }}
{{- if .Values.backups.velero.enabled }}
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: backup-velero
  labels:
    app: backup-velero
spec:
  schedule: {{ .Values.backups.velero.schedule | default "0 2 * * *" | quote }}
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app: backup-velero
        spec:
          serviceAccountName: {{ .Values.backups.serviceAccountName | default "backup" }}
          restartPolicy: OnFailure
          containers:
          - name: velero-backup
            image: "{{ .Values.backups.velero.image.repository }}:{{ .Values.backups.velero.image.tag }}"
            command:
            - /bin/sh
            - -c
            - |
              set -e
              echo "Installing rclone if needed..."
              if ! command -v rclone &> /dev/null; then
                apk add --no-cache curl unzip || apt-get update && apt-get install -y curl unzip
                curl https://rclone.org/install.sh | bash || echo "Failed to install rclone, continuing..."
              fi
              
              echo "Starting Velero backup..."
              
              # Create backup
              BACKUP_NAME="velero-backup-$(date +%Y%m%d-%H%M%S)"
              velero backup create $BACKUP_NAME --wait
              
              # Upload to Backblaze if configured
              {{- if .Values.backups.backblaze.enabled }}
              echo "Uploading Velero backup to Backblaze..."
              mkdir -p /root/.config/rclone
              cat > /root/.config/rclone/rclone.conf <<EOF
              [b2]
              type = b2
              account = $RCLONE_CONFIG_B2_ACCOUNT
              key = $RCLONE_CONFIG_B2_KEY
              EOF
              rclone copy /tmp/backups/velero b2:{{ .Values.backups.backblaze.bucket }}/velero/ --transfers=4 --checkers=8
              {{- end }}
              
              echo "Velero backup completed: $BACKUP_NAME"
            env:
            {{- if .Values.backups.backblaze.enabled }}
            - name: RCLONE_CONFIG_B2_TYPE
              value: b2
            - name: RCLONE_CONFIG_B2_ACCOUNT
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.backups.backblaze.secretName }}
                  key: {{ .Values.backups.backblaze.accountIdKey | default "account-id" }}
            - name: RCLONE_CONFIG_B2_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.backups.backblaze.secretName }}
                  key: {{ .Values.backups.backblaze.applicationKeyKey | default "application-key" }}
            {{- end }}
            resources:
              {{- if .Values.backups.velero.resources }}
              {{- toYaml .Values.backups.velero.resources | nindent 14 }}
              {{- else }}
              limits:
                memory: 512Mi
                cpu: 500m
              requests:
                memory: 256Mi
                cpu: 200m
              {{- end }}
{{- end }}

{{- if .Values.backups.etcd.enabled }}
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: backup-etcd
  labels:
    app: backup-etcd
spec:
  schedule: {{ .Values.backups.etcd.schedule | default "0 3 * * *" | quote }}
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app: backup-etcd
        spec:
          hostNetwork: true
          hostPID: true
          restartPolicy: OnFailure
          containers:
          - name: etcd-backup
            image: "{{ .Values.backups.etcd.image.repository }}:{{ .Values.backups.etcd.image.tag }}"
            command:
            - /bin/sh
            - -c
            - |
              set -e
              echo "Installing rclone if needed..."
              if ! command -v rclone &> /dev/null; then
                apk add --no-cache curl unzip || true
                # Download and install rclone manually (works with sh, no bash needed)
                ARCH=$(uname -m)
                if [ "$ARCH" = "x86_64" ] || [ "$ARCH" = "amd64" ]; then
                  RCLONE_ARCH="amd64"
                elif [ "$ARCH" = "aarch64" ] || [ "$ARCH" = "arm64" ]; then
                  RCLONE_ARCH="arm64"
                else
                  RCLONE_ARCH="amd64"
                fi
                curl -L https://downloads.rclone.org/rclone-current-linux-${RCLONE_ARCH}.zip -o /tmp/rclone.zip 2>/dev/null || echo "Failed to download rclone"
                unzip -q /tmp/rclone.zip -d /tmp 2>/dev/null || echo "Failed to unzip rclone"
                cp /tmp/rclone-*-linux-${RCLONE_ARCH}/rclone /usr/local/bin/ 2>/dev/null || echo "Failed to copy rclone"
                chmod +x /usr/local/bin/rclone 2>/dev/null || true
                # Cleanup
                rm -rf /tmp/rclone* 2>/dev/null || true
              fi
              
              echo "Starting etcd backup..."
              
              BACKUP_FILE="/tmp/backups/etcd/etcd-backup-$(date +%Y%m%d-%H%M%S).db"
              mkdir -p /tmp/backups/etcd
              
              # Backup etcd
              ETCD_ENDPOINTS="{{ .Values.backups.etcd.endpoints | default "https://127.0.0.1:2379" }}"
              ETCD_CA="{{ .Values.backups.etcd.ca | default "/var/lib/rancher/k3s/server/tls/etcd/server-ca.crt" }}"
              ETCD_CERT="{{ .Values.backups.etcd.cert | default "/var/lib/rancher/k3s/server/tls/etcd/server-client.crt" }}"
              ETCD_KEY="{{ .Values.backups.etcd.key | default "/var/lib/rancher/k3s/server/tls/etcd/server-client.key" }}"
              
              ETCDCTL_API=3 etcdctl \
                --endpoints=$ETCD_ENDPOINTS \
                --cacert=$ETCD_CA \
                --cert=$ETCD_CERT \
                --key=$ETCD_KEY \
                snapshot save $BACKUP_FILE
              
              echo "etcd backup created: $BACKUP_FILE"
              
              {{- if .Values.backups.backblaze.enabled }}
              echo "Uploading etcd backup to Backblaze..."
              mkdir -p /root/.config/rclone
              cat > /root/.config/rclone/rclone.conf <<EOF
              [b2]
              type = b2
              account = $RCLONE_CONFIG_B2_ACCOUNT
              key = $RCLONE_CONFIG_B2_KEY
              EOF
              rclone copy $BACKUP_FILE b2:{{ .Values.backups.backblaze.bucket }}/etcd/ --transfers=4 --checkers=8
              {{- end }}
              
              echo "etcd backup completed"
            securityContext:
              privileged: true
            volumeMounts:
            - name: etcd-data
              mountPath: /var/lib/rancher/k3s/server
              readOnly: true
            - name: tmp-backups
              mountPath: /tmp/backups
            env:
            {{- if .Values.backups.backblaze.enabled }}
            - name: RCLONE_CONFIG_B2_TYPE
              value: b2
            - name: RCLONE_CONFIG_B2_ACCOUNT
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.backups.backblaze.secretName }}
                  key: {{ .Values.backups.backblaze.accountIdKey | default "account-id" }}
            - name: RCLONE_CONFIG_B2_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.backups.backblaze.secretName }}
                  key: {{ .Values.backups.backblaze.applicationKeyKey | default "application-key" }}
            {{- end }}
            resources:
              {{- if .Values.backups.etcd.resources }}
              {{- toYaml .Values.backups.etcd.resources | nindent 14 }}
              {{- else }}
              limits:
                memory: 512Mi
                cpu: 500m
              requests:
                memory: 256Mi
                cpu: 200m
              {{- end }}
          volumes:
          - name: etcd-data
            hostPath:
              path: /var/lib/rancher/k3s/server
              type: Directory
          - name: tmp-backups
            emptyDir: {}
{{- end }}

{{- if .Values.backups.postgres.enabled }}
{{- range .Values.backups.postgres.databases }}
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: backup-postgres-{{ .name }}
  labels:
    app: backup-postgres
    database: {{ .name }}
spec:
  schedule: {{ .schedule | default "0 4 * * *" | quote }}
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app: backup-postgres
            database: {{ .name }}
        spec:
          restartPolicy: OnFailure
          containers:
          - name: postgres-backup
            image: "{{ $.Values.backups.postgres.image.repository }}:{{ $.Values.backups.postgres.image.tag }}"
            command:
            - /bin/sh
            - -c
            - |
              set -e
              echo "Installing rclone if needed..."
              if ! command -v rclone &> /dev/null; then
                apk add --no-cache curl unzip || true
                # Download and install rclone manually (works with sh, no bash needed)
                ARCH=$(uname -m)
                if [ "$ARCH" = "x86_64" ] || [ "$ARCH" = "amd64" ]; then
                  RCLONE_ARCH="amd64"
                elif [ "$ARCH" = "aarch64" ] || [ "$ARCH" = "arm64" ]; then
                  RCLONE_ARCH="arm64"
                else
                  RCLONE_ARCH="amd64"
                fi
                curl -L https://downloads.rclone.org/rclone-current-linux-${RCLONE_ARCH}.zip -o /tmp/rclone.zip 2>/dev/null || echo "Failed to download rclone"
                unzip -q /tmp/rclone.zip -d /tmp 2>/dev/null || echo "Failed to unzip rclone"
                cp /tmp/rclone-*-linux-${RCLONE_ARCH}/rclone /usr/local/bin/ 2>/dev/null || echo "Failed to copy rclone"
                chmod +x /usr/local/bin/rclone 2>/dev/null || true
                # Cleanup
                rm -rf /tmp/rclone* 2>/dev/null || true
              fi
              
              echo "Starting PostgreSQL backup for {{ .name }}..."
              
              BACKUP_FILE="/tmp/backups/postgres/{{ .name }}-$(date +%Y%m%d-%H%M%S).sql.gz"
              mkdir -p /tmp/backups/postgres
              
              # Create backup
              PGPASSWORD="$POSTGRES_PASSWORD" pg_dump \
                -h {{ .host }} \
                -p {{ .port | default "5432" }} \
                -U {{ .user }} \
                -d {{ .database }} \
                {{- if .options }}
                {{ .options }}
                {{- else }}
                --clean --if-exists
                {{- end }} | gzip > $BACKUP_FILE
              
              echo "PostgreSQL backup created: $BACKUP_FILE"
              
              {{- if $.Values.backups.backblaze.enabled }}
              echo "Uploading PostgreSQL backup to Backblaze..."
              mkdir -p /root/.config/rclone
              cat > /root/.config/rclone/rclone.conf <<EOF
              [b2]
              type = b2
              account = $RCLONE_CONFIG_B2_ACCOUNT
              key = $RCLONE_CONFIG_B2_KEY
              EOF
              rclone copy $BACKUP_FILE b2:{{ $.Values.backups.backblaze.bucket }}/postgres/{{ .name }}/ --transfers=4 --checkers=8
              {{- end }}
              
              echo "PostgreSQL backup completed for {{ .name }}"
            env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .secretName }}
                  key: {{ .passwordKey | default "postgres-password" }}
            {{- if $.Values.backups.backblaze.enabled }}
            - name: RCLONE_CONFIG_B2_TYPE
              value: b2
            - name: RCLONE_CONFIG_B2_ACCOUNT
              valueFrom:
                secretKeyRef:
                  name: {{ $.Values.backups.backblaze.secretName }}
                  key: {{ $.Values.backups.backblaze.accountIdKey | default "account-id" }}
            - name: RCLONE_CONFIG_B2_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ $.Values.backups.backblaze.secretName }}
                  key: {{ $.Values.backups.backblaze.applicationKeyKey | default "application-key" }}
            {{- end }}
            resources:
              {{- if $.Values.backups.postgres.resources }}
              {{- toYaml $.Values.backups.postgres.resources | nindent 14 }}
              {{- else }}
              limits:
                memory: 512Mi
                cpu: 500m
              requests:
                memory: 256Mi
                cpu: 200m
              {{- end }}
          volumes:
          - name: tmp-backups
            emptyDir: {}
{{- end }}
{{- end }}

{{- if .Values.backups.mysql.enabled }}
{{- range .Values.backups.mysql.databases }}
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: backup-mysql-{{ .name }}
  labels:
    app: backup-mysql
    database: {{ .name }}
spec:
  schedule: {{ .schedule | default "0 5 * * *" | quote }}
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app: backup-mysql
            database: {{ .name }}
        spec:
          restartPolicy: OnFailure
          containers:
          - name: mysql-backup
            image: "{{ $.Values.backups.mysql.image.repository }}:{{ $.Values.backups.mysql.image.tag }}"
            command:
            - /bin/sh
            - -c
            - |
              set -e
              echo "Installing rclone if needed..."
              if ! command -v rclone &> /dev/null; then
                apt-get update && apt-get install -y curl unzip || apk add --no-cache curl unzip
                curl https://rclone.org/install.sh | bash || echo "Failed to install rclone, continuing..."
              fi
              
              echo "Starting MySQL backup for {{ .name }}..."
              
              BACKUP_FILE="/tmp/backups/mysql/{{ .name }}-$(date +%Y%m%d-%H%M%S).sql.gz"
              mkdir -p /tmp/backups/mysql
              
              # Create backup
              mysqldump \
                -h {{ .host }} \
                -P {{ .port | default "3306" }} \
                -u {{ .user }} \
                -p"$MYSQL_PASSWORD" \
                {{ .database }} \
                {{- if .options }}
                {{ .options }}
                {{- else }}
                --single-transaction --routines --triggers
                {{- end }} | gzip > $BACKUP_FILE
              
              echo "MySQL backup created: $BACKUP_FILE"
              
              {{- if $.Values.backups.backblaze.enabled }}
              echo "Uploading MySQL backup to Backblaze..."
              mkdir -p /root/.config/rclone
              cat > /root/.config/rclone/rclone.conf <<EOF
              [b2]
              type = b2
              account = $RCLONE_CONFIG_B2_ACCOUNT
              key = $RCLONE_CONFIG_B2_KEY
              EOF
              rclone copy $BACKUP_FILE b2:{{ $.Values.backups.backblaze.bucket }}/mysql/{{ .name }}/ --transfers=4 --checkers=8
              {{- end }}
              
              echo "MySQL backup completed for {{ .name }}"
            env:
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .secretName }}
                  key: {{ .passwordKey | default "mysql-password" }}
            {{- if $.Values.backups.backblaze.enabled }}
            - name: RCLONE_CONFIG_B2_TYPE
              value: b2
            - name: RCLONE_CONFIG_B2_ACCOUNT
              valueFrom:
                secretKeyRef:
                  name: {{ $.Values.backups.backblaze.secretName }}
                  key: {{ $.Values.backups.backblaze.accountIdKey | default "account-id" }}
            - name: RCLONE_CONFIG_B2_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ $.Values.backups.backblaze.secretName }}
                  key: {{ $.Values.backups.backblaze.applicationKeyKey | default "application-key" }}
            {{- end }}
            resources:
              {{- if $.Values.backups.mysql.resources }}
              {{- toYaml $.Values.backups.mysql.resources | nindent 14 }}
              {{- else }}
              limits:
                memory: 512Mi
                cpu: 500m
              requests:
                memory: 256Mi
                cpu: 200m
              {{- end }}
          volumes:
          - name: tmp-backups
            emptyDir: {}
{{- end }}
{{- end }}

{{- if .Values.backups.meilisearch.enabled }}
{{- range .Values.backups.meilisearch.instances }}
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: backup-meilisearch-{{ .name }}
  labels:
    app: backup-meilisearch
    instance: {{ .name }}
spec:
  schedule: {{ .schedule | default "0 6 * * *" | quote }}
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app: backup-meilisearch
            instance: {{ .name }}
        spec:
          restartPolicy: OnFailure
          securityContext:
            runAsUser: 0
            runAsGroup: 0
          containers:
          - name: meilisearch-backup
            image: "{{ $.Values.backups.meilisearch.image.repository }}:{{ $.Values.backups.meilisearch.image.tag }}"
            securityContext:
              runAsUser: 0
              runAsGroup: 0
            command:
            - /bin/sh
            - -c
            - |
              set -e
              echo "Installing rclone and curl if needed..."
              if ! command -v rclone &> /dev/null; then
                apk add --no-cache curl unzip || true
                # Download and install rclone manually (works with sh, no bash needed)
                ARCH=$(uname -m)
                if [ "$ARCH" = "x86_64" ] || [ "$ARCH" = "amd64" ]; then
                  RCLONE_ARCH="amd64"
                elif [ "$ARCH" = "aarch64" ] || [ "$ARCH" = "arm64" ]; then
                  RCLONE_ARCH="arm64"
                else
                  RCLONE_ARCH="amd64"
                fi
                curl -L https://downloads.rclone.org/rclone-current-linux-${RCLONE_ARCH}.zip -o /tmp/rclone.zip 2>/dev/null || echo "Failed to download rclone"
                unzip -q /tmp/rclone.zip -d /tmp 2>/dev/null || echo "Failed to unzip rclone"
                cp /tmp/rclone-*-linux-${RCLONE_ARCH}/rclone /usr/local/bin/ 2>/dev/null || echo "Failed to copy rclone"
                chmod +x /usr/local/bin/rclone 2>/dev/null || true
                # Cleanup
                rm -rf /tmp/rclone* 2>/dev/null || true
              fi
              
              echo "Starting Meilisearch backup for {{ .name }}..."
              
              BACKUP_FILE="/tmp/backups/meilisearch/{{ .name }}-$(date +%Y%m%d-%H%M%S).tar.gz"
              mkdir -p /tmp/backups/meilisearch
              
              # Trigger Meilisearch dump
              MEILI_URL="http://{{ .host }}:{{ .port | default "7700" }}"
              MEILI_KEY="$MEILI_MASTER_KEY"
              
              # Create a snapshot/dump using Meilisearch API
              curl -X POST "$MEILI_URL/snapshots" \
                -H "Authorization: Bearer $MEILI_KEY" \
                || echo "Note: Snapshot API may not be available, using alternative method"
              
              # Wait a moment for snapshot to be created
              sleep 5
              
              # If we have access to the data directory, backup it directly
              # Otherwise, we'll need to use the export API if available
              echo "Meilisearch backup initiated for {{ .name }}"
              
              {{- if $.Values.backups.backblaze.enabled }}
              echo "Uploading Meilisearch backup to Backblaze..."
              # Note: Actual backup method depends on Meilisearch version and access
              mkdir -p /root/.config/rclone
              cat > /root/.config/rclone/rclone.conf <<EOF
              [b2]
              type = b2
              account = $RCLONE_CONFIG_B2_ACCOUNT
              key = $RCLONE_CONFIG_B2_KEY
              EOF
              rclone copy /tmp/backups/meilisearch/ b2:{{ $.Values.backups.backblaze.bucket }}/meilisearch/{{ .name }}/ --transfers=4 --checkers=8 || true
              {{- end }}
              
              echo "Meilisearch backup completed for {{ .name }}"
            env:
            {{- if .masterKeySecret }}
            - name: MEILI_MASTER_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .masterKeySecret.name }}
                  key: {{ .masterKeySecret.key }}
            {{- else if .masterKey }}
            - name: MEILI_MASTER_KEY
              value: {{ .masterKey | quote }}
            {{- end }}
            {{- if $.Values.backups.backblaze.enabled }}
            - name: RCLONE_CONFIG_B2_TYPE
              value: b2
            - name: RCLONE_CONFIG_B2_ACCOUNT
              valueFrom:
                secretKeyRef:
                  name: {{ $.Values.backups.backblaze.secretName }}
                  key: {{ $.Values.backups.backblaze.accountIdKey | default "account-id" }}
            - name: RCLONE_CONFIG_B2_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ $.Values.backups.backblaze.secretName }}
                  key: {{ $.Values.backups.backblaze.applicationKeyKey | default "application-key" }}
            {{- end }}
            resources:
              {{- if $.Values.backups.meilisearch.resources }}
              {{- toYaml $.Values.backups.meilisearch.resources | nindent 14 }}
              {{- else }}
              limits:
                memory: 512Mi
                cpu: 500m
              requests:
                memory: 256Mi
                cpu: 200m
              {{- end }}
          volumes:
          - name: tmp-backups
            emptyDir: {}
{{- end }}
{{- end }}

{{- if .Values.backups.redis.enabled }}
{{- range .Values.backups.redis.instances }}
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: backup-redis-{{ .name }}
  labels:
    app: backup-redis
    instance: {{ .name }}
spec:
  schedule: {{ .schedule | default "0 7 * * *" | quote }}
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app: backup-redis
            instance: {{ .name }}
        spec:
          restartPolicy: OnFailure
          securityContext:
            runAsUser: 0
            runAsGroup: 0
          containers:
          - name: redis-backup
            image: "{{ $.Values.backups.redis.image.repository }}:{{ $.Values.backups.redis.image.tag }}"
            securityContext:
              runAsUser: 0
              runAsGroup: 0
            command:
            - /bin/sh
            - -c
            - |
              set -e
              echo "Installing rclone if needed..."
              if ! command -v rclone &> /dev/null; then
                apk add --no-cache curl unzip || true
                # Download and install rclone manually (works with sh, no bash needed)
                curl -O https://downloads.rclone.org/rclone-current-linux-amd64.zip 2>/dev/null || \
                curl -O https://downloads.rclone.org/rclone-current-linux-arm64.zip 2>/dev/null || \
                echo "Failed to download rclone"
                unzip -q rclone-*-linux-*.zip 2>/dev/null || echo "Failed to unzip rclone"
                cp rclone-*-linux-*/rclone /usr/local/bin/ 2>/dev/null || echo "Failed to copy rclone"
                chmod +x /usr/local/bin/rclone 2>/dev/null || true
                # Cleanup
                rm -rf rclone-* 2>/dev/null || true
              fi
              
              echo "Starting Redis backup for {{ .name }}..."
              
              BACKUP_FILE="/tmp/backups/redis/{{ .name }}-$(date +%Y%m%d-%H%M%S).rdb"
              mkdir -p /tmp/backups/redis
              
              # Create backup using redis-cli
              redis-cli \
                -h {{ .host }} \
                -p {{ .port | default "6379" }} \
                {{- if .password }}
                -a "$REDIS_PASSWORD" \
                {{- end }}
                --rdb $BACKUP_FILE
              
              # Compress the backup
              gzip $BACKUP_FILE
              
              echo "Redis backup created: $BACKUP_FILE.gz"
              
              {{- if $.Values.backups.backblaze.enabled }}
              echo "Uploading Redis backup to Backblaze..."
              mkdir -p /root/.config/rclone
              cat > /root/.config/rclone/rclone.conf <<EOF
              [b2]
              type = b2
              account = $RCLONE_CONFIG_B2_ACCOUNT
              key = $RCLONE_CONFIG_B2_KEY
              EOF
              rclone copy $BACKUP_FILE.gz b2:{{ $.Values.backups.backblaze.bucket }}/redis/{{ .name }}/ --transfers=4 --checkers=8
              {{- end }}
              
              echo "Redis backup completed for {{ .name }}"
            env:
            {{- if .password }}
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .passwordSecret.name }}
                  key: {{ .passwordSecret.key }}
            {{- end }}
            {{- if $.Values.backups.backblaze.enabled }}
            - name: RCLONE_CONFIG_B2_TYPE
              value: b2
            - name: RCLONE_CONFIG_B2_ACCOUNT
              valueFrom:
                secretKeyRef:
                  name: {{ $.Values.backups.backblaze.secretName }}
                  key: {{ $.Values.backups.backblaze.accountIdKey | default "account-id" }}
            - name: RCLONE_CONFIG_B2_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ $.Values.backups.backblaze.secretName }}
                  key: {{ $.Values.backups.backblaze.applicationKeyKey | default "application-key" }}
            {{- end }}
            resources:
              {{- if $.Values.backups.redis.resources }}
              {{- toYaml $.Values.backups.redis.resources | nindent 14 }}
              {{- else }}
              limits:
                memory: 512Mi
                cpu: 500m
              requests:
                memory: 256Mi
                cpu: 200m
              {{- end }}
          volumes:
          - name: tmp-backups
            emptyDir: {}
{{- end }}
{{- end }}
{{- end }}

{{- if .Values.backups.immichData.enabled }}
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: backup-immich-data
  labels:
    app: backup-immich-data
spec:
  schedule: {{ .Values.backups.immichData.schedule | default "0 */6 * * *" | quote }}
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app: backup-immich-data
        spec:
          restartPolicy: OnFailure
          containers:
          - name: immich-data-backup
            image: "{{ .Values.backups.immichData.image.repository }}:{{ .Values.backups.immichData.image.tag }}"
            command:
            - /bin/sh
            - -c
            - |
              set -e
              echo "Installing rclone if needed..."
              if ! command -v rclone &> /dev/null; then
                apk add --no-cache curl unzip || true
                # Download and install rclone manually (works with sh, no bash needed)
                ARCH=$(uname -m)
                if [ "$ARCH" = "x86_64" ] || [ "$ARCH" = "amd64" ]; then
                  RCLONE_ARCH="amd64"
                elif [ "$ARCH" = "aarch64" ] || [ "$ARCH" = "arm64" ]; then
                  RCLONE_ARCH="arm64"
                else
                  RCLONE_ARCH="amd64"
                fi
                curl -L https://downloads.rclone.org/rclone-current-linux-${RCLONE_ARCH}.zip -o /tmp/rclone.zip 2>/dev/null || echo "Failed to download rclone"
                unzip -q /tmp/rclone.zip -d /tmp 2>/dev/null || echo "Failed to unzip rclone"
                cp /tmp/rclone-*-linux-${RCLONE_ARCH}/rclone /usr/local/bin/ 2>/dev/null || echo "Failed to copy rclone"
                chmod +x /usr/local/bin/rclone 2>/dev/null || true
                # Cleanup
                rm -rf /tmp/rclone* 2>/dev/null || true
              fi
              
              echo "Starting Immich data directory backup..."
              
              # Setup rclone config
              mkdir -p /root/.config/rclone
              cat > /root/.config/rclone/rclone.conf <<EOF
              [b2]
              type = b2
              account = $RCLONE_CONFIG_B2_ACCOUNT
              key = $RCLONE_CONFIG_B2_KEY
              EOF
              
              # Sync Immich upload directory to Backblaze (incremental)
              # Using sync instead of copy to only transfer new/changed files
              echo "Syncing Immich data directory to Backblaze..."
              rclone sync /immich-data b2:{{ .Values.backups.backblaze.bucket }}/immich-data/ \
                --transfers={{ .Values.backups.immichData.transfers | default "4" }} \
                --checkers={{ .Values.backups.immichData.checkers | default "8" }} \
                --log-level=INFO \
                --stats=1m \
                --stats-log-level=INFO
              
              echo "Immich data backup completed"
            env:
            {{- if .Values.backups.backblaze.enabled }}
            - name: RCLONE_CONFIG_B2_ACCOUNT
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.backups.backblaze.secretName }}
                  key: {{ .Values.backups.backblaze.accountIdKey | default "account-id" }}
            - name: RCLONE_CONFIG_B2_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.backups.backblaze.secretName }}
                  key: {{ .Values.backups.backblaze.applicationKeyKey | default "application-key" }}
            {{- end }}
            resources:
              {{- if .Values.backups.immichData.resources }}
              {{- toYaml .Values.backups.immichData.resources | nindent 14 }}
              {{- else }}
              limits:
                memory: 2Gi
                cpu: 1000m
              requests:
                memory: 512Mi
                cpu: 500m
              {{- end }}
            volumeMounts:
            - name: immich-data
              mountPath: /immich-data
              readOnly: true
          volumes:
          - name: immich-data
            hostPath:
              path: {{ .Values.backups.immichData.sourcePath }}
              type: Directory
{{- end }}

{{- if .Values.backups.nextcloudData.enabled }}
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: backup-nextcloud-data
  labels:
    app: backup-nextcloud-data
spec:
  schedule: {{ .Values.backups.nextcloudData.schedule | default "0 10 * * *" | quote }}
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app: backup-nextcloud-data
        spec:
          restartPolicy: OnFailure
          containers:
          - name: nextcloud-data-backup
            image: "{{ .Values.backups.nextcloudData.image.repository }}:{{ .Values.backups.nextcloudData.image.tag }}"
            command:
            - /bin/sh
            - -c
            - |
              set -e
              echo "Installing rclone if needed..."
              if ! command -v rclone &> /dev/null; then
                apk add --no-cache curl unzip || true
                # Download and install rclone manually (works with sh, no bash needed)
                ARCH=$(uname -m)
                if [ "$ARCH" = "x86_64" ] || [ "$ARCH" = "amd64" ]; then
                  RCLONE_ARCH="amd64"
                elif [ "$ARCH" = "aarch64" ] || [ "$ARCH" = "arm64" ]; then
                  RCLONE_ARCH="arm64"
                else
                  RCLONE_ARCH="amd64"
                fi
                curl -L https://downloads.rclone.org/rclone-current-linux-${RCLONE_ARCH}.zip -o /tmp/rclone.zip 2>/dev/null || echo "Failed to download rclone"
                unzip -q /tmp/rclone.zip -d /tmp 2>/dev/null || echo "Failed to unzip rclone"
                cp /tmp/rclone-*-linux-${RCLONE_ARCH}/rclone /usr/local/bin/ 2>/dev/null || echo "Failed to copy rclone"
                chmod +x /usr/local/bin/rclone 2>/dev/null || true
                # Cleanup
                rm -rf /tmp/rclone* 2>/dev/null || true
              fi
              
              echo "Starting Nextcloud data directory backup..."
              
              # Setup rclone config
              mkdir -p /root/.config/rclone
              cat > /root/.config/rclone/rclone.conf <<EOF
              [b2]
              type = b2
              account = $RCLONE_CONFIG_B2_ACCOUNT
              key = $RCLONE_CONFIG_B2_KEY
              EOF
              
              # Sync Nextcloud data directory to Backblaze (incremental)
              # Using sync instead of copy to only transfer new/changed files
              echo "Syncing Nextcloud data directory to Backblaze..."
              rclone sync /nextcloud-data b2:{{ .Values.backups.backblaze.bucket }}/nextcloud-data/ \
                --transfers={{ .Values.backups.nextcloudData.transfers | default "4" }} \
                --checkers={{ .Values.backups.nextcloudData.checkers | default "8" }} \
                --log-level=INFO \
                --stats=1m \
                --stats-log-level=INFO
              
              echo "Nextcloud data backup completed"
            env:
            {{- if .Values.backups.backblaze.enabled }}
            - name: RCLONE_CONFIG_B2_ACCOUNT
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.backups.backblaze.secretName }}
                  key: {{ .Values.backups.backblaze.accountIdKey | default "account-id" }}
            - name: RCLONE_CONFIG_B2_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.backups.backblaze.secretName }}
                  key: {{ .Values.backups.backblaze.applicationKeyKey | default "application-key" }}
            {{- end }}
            resources:
              {{- if .Values.backups.nextcloudData.resources }}
              {{- toYaml .Values.backups.nextcloudData.resources | nindent 14 }}
              {{- else }}
              limits:
                memory: 2Gi
                cpu: 1000m
              requests:
                memory: 512Mi
                cpu: 500m
              {{- end }}
            volumeMounts:
            - name: nextcloud-data
              mountPath: /nextcloud-data
              readOnly: true
          volumes:
          - name: nextcloud-data
            hostPath:
              path: {{ .Values.backups.nextcloudData.sourcePath }}
              type: Directory
{{- end }}

{{- if .Values.backups.cleanup.enabled }}
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: backup-cleanup
  labels:
    app: backup-cleanup
spec:
  schedule: {{ .Values.backups.cleanup.schedule | default "0 8 * * *" | quote }}
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app: backup-cleanup
        spec:
          restartPolicy: OnFailure
          containers:
          - name: cleanup
            image: "{{ .Values.backups.cleanup.image.repository }}:{{ .Values.backups.cleanup.image.tag }}"
            command:
            - /bin/sh
            - -c
            - |
              set -e
              echo "Installing rclone if needed..."
              if ! command -v rclone &> /dev/null; then
                apk add --no-cache curl unzip || true
                # Download and install rclone manually (works with sh, no bash needed)
                ARCH=$(uname -m)
                if [ "$ARCH" = "x86_64" ] || [ "$ARCH" = "amd64" ]; then
                  RCLONE_ARCH="amd64"
                elif [ "$ARCH" = "aarch64" ] || [ "$ARCH" = "arm64" ]; then
                  RCLONE_ARCH="arm64"
                else
                  RCLONE_ARCH="amd64"
                fi
                curl -L https://downloads.rclone.org/rclone-current-linux-${RCLONE_ARCH}.zip -o /tmp/rclone.zip 2>/dev/null || echo "Failed to download rclone"
                unzip -q /tmp/rclone.zip -d /tmp 2>/dev/null || echo "Failed to unzip rclone"
                cp /tmp/rclone-*-linux-${RCLONE_ARCH}/rclone /usr/local/bin/ 2>/dev/null || echo "Failed to copy rclone"
                chmod +x /usr/local/bin/rclone 2>/dev/null || true
                # Cleanup
                rm -rf /tmp/rclone* 2>/dev/null || true
              fi
              
              echo "Installing Python if needed..."
              if ! command -v python3 &> /dev/null; then
                apk add --no-cache python3 py3-pip
              fi
              
              python3 << 'PYEOF'
              import os
              import re
              import subprocess
              from datetime import datetime, timedelta
              from collections import defaultdict
              
              # Configuration
              B2_BUCKET = "{{ .Values.backups.backblaze.bucket }}"
              KEEP_DAILY_DAYS = 7
              KEEP_MONTHLY_MONTHS = 12
              
              # Setup rclone config
              os.makedirs("/root/.config/rclone", exist_ok=True)
              with open("/root/.config/rclone/rclone.conf", "w") as f:
                  f.write("[b2]\n")
                  f.write("type = b2\n")
                  f.write("account = " + os.environ.get("RCLONE_CONFIG_B2_ACCOUNT", "") + "\n")
                  f.write("key = " + os.environ.get("RCLONE_CONFIG_B2_KEY", "") + "\n")
              
              def parse_date_from_filename(filename):
                  """Extract date from filename. Supports formats like:
                  - YYYYMMDD-HHMMSS (e.g., 20240103-023042)
                  - YYYYMMDD (e.g., 20240103)
                  """
                  # Try YYYYMMDD-HHMMSS format
                  match = re.search(r'(\d{8})-(\d{6})', filename)
                  if match:
                      date_str = match.group(1)
                      try:
                          return datetime.strptime(date_str, "%Y%m%d")
                      except ValueError:
                          pass
                  
                  # Try YYYYMMDD format
                  match = re.search(r'(\d{8})', filename)
                  if match:
                      date_str = match.group(1)
                      try:
                          return datetime.strptime(date_str, "%Y%m%d")
                      except ValueError:
                          pass
                  
                  return None
              
              def list_files_in_folder(folder_path):
                  """List all files in a B2 folder using rclone."""
                  try:
                      result = subprocess.run(
                          ["rclone", "ls", f"b2:{B2_BUCKET}/{folder_path}/"],
                          capture_output=True,
                          text=True,
                          check=True
                      )
                      files = []
                      for line in result.stdout.strip().split("\n"):
                          if line:
                              parts = line.split(None, 1)
                              if len(parts) == 2:
                                  size, filename = parts
                                  files.append(filename)
                      return files
                  except subprocess.CalledProcessError as e:
                      print(f"Error listing files in {folder_path}: {e.stderr}")
                      return []
              
              def delete_file(folder_path, filename):
                  """Delete a file from B2 using rclone."""
                  try:
                      subprocess.run(
                          ["rclone", "delete", f"b2:{B2_BUCKET}/{folder_path}/{filename}"],
                          check=True,
                          capture_output=True
                      )
                      return True
                  except subprocess.CalledProcessError as e:
                      print(f"Error deleting {folder_path}/{filename}: {e.stderr}")
                      return False
              
              def cleanup_folder(folder_path):
                  """Clean up backups in a folder according to retention policy."""
                  print(f"\n{'='*80}")
                  print(f"Cleaning up folder: {folder_path}")
                  print(f"{'='*80}")
                  
                  files = list_files_in_folder(folder_path)
                  if not files:
                      print(f"No files found in {folder_path}")
                      return
                  
                  print(f"Found {len(files)} files")
                  
                  # Parse dates and group files
                  files_with_dates = []
                  for filename in files:
                      date = parse_date_from_filename(filename)
                      if date:
                          files_with_dates.append((filename, date))
                      else:
                          print(f"Warning: Could not parse date from {filename}, skipping")
                  
                  if not files_with_dates:
                      print("No files with parseable dates found")
                      return
                  
                  # Sort by date (newest first)
                  files_with_dates.sort(key=lambda x: x[1], reverse=True)
                  
                  now = datetime.now()
                  cutoff_daily = now - timedelta(days=KEEP_DAILY_DAYS)
                  cutoff_monthly = now - timedelta(days=KEEP_DAILY_DAYS + (KEEP_MONTHLY_MONTHS * 30))
                  
                  # Group by day for daily retention
                  daily_groups = defaultdict(list)
                  monthly_groups = defaultdict(list)
                  to_delete = []
                  
                  for filename, date in files_with_dates:
                      if date >= cutoff_daily:
                          # Keep daily backups
                          day_key = date.strftime("%Y-%m-%d")
                          daily_groups[day_key].append((filename, date))
                      elif date >= cutoff_monthly:
                          # Keep monthly backups
                          month_key = date.strftime("%Y-%m")
                          monthly_groups[month_key].append((filename, date))
                      else:
                          # Too old, mark for deletion
                          to_delete.append((filename, date))
                  
                  # Keep only the newest file per day for daily backups
                  to_keep = set()
                  for day, files in daily_groups.items():
                      if files:
                          # Keep the newest file for this day
                          newest = max(files, key=lambda x: x[1])
                          to_keep.add(newest[0])
                          # Mark others for deletion
                          for filename, date in files:
                              if filename != newest[0]:
                                  to_delete.append((filename, date))
                  
                  # Keep only the newest file per month for monthly backups
                  for month, files in monthly_groups.items():
                      if files:
                          # Keep the newest file for this month
                          newest = max(files, key=lambda x: x[1])
                          to_keep.add(newest[0])
                          # Mark others for deletion
                          for filename, date in files:
                              if filename != newest[0]:
                                  to_delete.append((filename, date))
                  
                  # Delete files
                  deleted_count = 0
                  kept_count = len(to_keep)
                  
                  for filename, date in to_delete:
                      if delete_file(folder_path, filename):
                          deleted_count += 1
                          print(f"Deleted: {filename} (date: {date.strftime('%Y-%m-%d')})")
                  
                  print(f"\nSummary for {folder_path}:")
                  print(f"  Kept: {kept_count} files")
                  print(f"  Deleted: {deleted_count} files")
              
              # Cleanup all backup folders
              folders_to_clean = []
              
              {{- if .Values.backups.velero.enabled }}
              folders_to_clean.append("velero")
              {{- end }}
              
              {{- if .Values.backups.etcd.enabled }}
              folders_to_clean.append("etcd")
              {{- end }}
              
              # Add postgres databases
              {{- if .Values.backups.postgres.enabled }}
              {{- range .Values.backups.postgres.databases }}
              folders_to_clean.append("postgres/{{ .name }}")
              {{- end }}
              {{- end }}
              
              # Add mysql databases
              {{- if .Values.backups.mysql.enabled }}
              {{- range .Values.backups.mysql.databases }}
              folders_to_clean.append("mysql/{{ .name }}")
              {{- end }}
              {{- end }}
              
              # Add meilisearch instances
              {{- if .Values.backups.meilisearch.enabled }}
              {{- range .Values.backups.meilisearch.instances }}
              folders_to_clean.append("meilisearch/{{ .name }}")
              {{- end }}
              {{- end }}
              
              # Add redis instances
              {{- if .Values.backups.redis.enabled }}
              {{- range .Values.backups.redis.instances }}
              folders_to_clean.append("redis/{{ .name }}")
              {{- end }}
              {{- end }}
              
              print("Starting backup cleanup...")
              print(f"Retention policy: {KEEP_DAILY_DAYS} days daily, {KEEP_MONTHLY_MONTHS} months monthly")
              
              for folder in folders_to_clean:
                  try:
                      cleanup_folder(folder)
                  except Exception as e:
                      print(f"Error cleaning up {folder}: {e}")
                      continue
              
              print("\n" + "="*80)
              print("Backup cleanup completed")
              print("="*80)
              PYEOF
            env:
            {{- if .Values.backups.backblaze.enabled }}
            - name: RCLONE_CONFIG_B2_ACCOUNT
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.backups.backblaze.secretName }}
                  key: {{ .Values.backups.backblaze.accountIdKey | default "account-id" }}
            - name: RCLONE_CONFIG_B2_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.backups.backblaze.secretName }}
                  key: {{ .Values.backups.backblaze.applicationKeyKey | default "application-key" }}
            {{- end }}
            resources:
              {{- if .Values.backups.cleanup.resources }}
              {{- toYaml .Values.backups.cleanup.resources | nindent 14 }}
              {{- else }}
              limits:
                memory: 512Mi
                cpu: 500m
              requests:
                memory: 256Mi
                cpu: 200m
              {{- end }}
{{- end }}
